import React, { useState } from "react";

const NavBar = (props) => {
    return (
        <nav>
            <h3>
                Music Library
            </h3>
        </nav>
    );
};

export default NavBar;